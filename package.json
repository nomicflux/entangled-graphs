{
  "name": "entangled-graphs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "test:compile": "rm -rf .tmp-test && tsc -p tsconfig.test.json && node -e \"require('fs').writeFileSync('.tmp-test/package.json', JSON.stringify({ type: 'commonjs' }))\"",
    "test": "npm run -s test:compile && node --test tests/*.test.cjs",
    "test:watch": "npm run -s test:compile && node --test --watch tests/*.test.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "@vitejs/plugin-vue": "^5.2.4",
    "typescript": "^5.9.3",
    "vite": "^6.1.0"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "vue": "^3.5.13"
  }
}
